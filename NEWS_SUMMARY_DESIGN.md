# 咚咚機器人：新聞閱讀與摘要功能設計方案

## 1. 核心架構與第三方服務
*   **網頁內容抓取**：使用 **Jina Reader API** (`https://r.jina.ai/`) 將使用者提供的新聞網址轉換為乾淨的 Markdown 格式，自動剔除廣告與無用導覽列。
    *   *初期營運*：不帶 API Key，使用預設免費用量（20 RPM）。
    *   *後續擴展準備*：若日後超出限制，可申請免費 API Key（提升至 500 RPM），甚至儲值付費。
*   **摘要生成**：將清洗後的 Markdown 內容送交 **Google Gemini API** 進行重點摘要。

## 2. 邊界情況處理：非新聞連結過濾 Pipeline
為防止系統處理購物網站、檔案連結或無效網頁，採取多層過濾機制：
1.  **廉價過濾器 (Cheap Filter)**：利用輕量級機制檢查網址（例如檢驗 Meta OG 標籤，或評估 Jina 回傳的 Markdown 字數是否過低）。
2.  **LLM 意圖防禦**：在 Prompt 內明文規定，若判斷為非新聞網頁，則不進行摘要，並溫和提示使用者「請提供有效的新聞連結」。

## 3. 系統擴展性與快取設計 (Cache)
*   **Cache Key 正規化**：剔除網址後方的 Tracking Codes（如 `?utm_source=...` 或 `&fbclid=...`），以最乾淨的 URL 作為 Cache Key。
*   **快取存活時間 (TTL)**：考量到突發新聞的滾動式更新，設定為 **15~30 分鐘** 左右。既可防禦同時間的爆發流量，也能保持內容新鮮度。

## 4. 監控與用量警告機制（擴展指標）
*   **Jina API 額度監控**：在呼叫 Jina Reader API 的邏輯中加入 HTTP 狀態碼檢測。若收到 `HTTP 429 Too Many Requests`，系統需打印明確的 Warning Log：
    `[Warning] Jina Reader API 已達使用次數上限 (HTTP 429)。請評估申請免費 API Key 或進入付費階段。`
*   此 Log 將幫助維運人員即時掌握是否需要移動到下一個營運階段。

## 5. 使用者體驗 (UX) 設計
*   **非同步流程**：收到連結後，立即使用 **Reply API** 回覆：「新聞內容正在擷取與整理中，請稍候...」（可搭配 LINE 的 Loading Animation 動畫），將使用者焦慮降至最低。
*   **結果推播**：待背景任務（抓取 + LLM 運算）完成後，再使用 **Push API** 將摘要結果推送給使用者。

## 6. Prompt Engineering 與幻覺防禦
### System Prompt 模板 (專為手機端閱讀設計)
```text
# Role
你是一個專為手機通訊軟體（LINE）設計的專業新聞摘要助理。你的目標是將長篇新聞濃縮成「一滑就能看完」的精華，並保持客觀、精煉的語氣。

# Constraints
1. 絕對精簡：總字數嚴格限制在 200 字以內。
2. 禁止廢話：不要輸出任何開場白或結尾問候，直接輸出摘要內容。
3. 強制留白：每個段落與條列項目之間，必須強制保留一個空白行（\n\n），絕不能讓文字擠在一起。
4. 封閉文本限制：你只能依據我提供的文本內容進行摘要，絕對禁止使用預先訓練知識進行補充。如果文本未提及，請保持沉默。
5. 數據絕對忠實：所有數字、金額、百分比、日期必須「100% 照抄原文」，禁止四捨五入或單位換算。
6. 客觀轉述視角：面對主觀評論，請使用「作者指出」、「報導宣稱」等轉述語氣。

# Output Format (強制填空)
📰 **[用一句話總結新聞主旨，不超過 20 個字]**

📌 **核心重點：**

🔹 [重點一：濃縮成 1-2 句話，直擊核心數字或事實]

🔹 [重點二：濃縮成 1-2 句話，說明背景或衝突點]

🔹 [重點三：濃縮成 1-2 句話，補充關鍵細節]

💡 **後續關注：**
[用一句話總結這件事的影響，或未來該注意什麼]
```
